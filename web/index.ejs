<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>python-server api docs</title>
</head>
<body>
<%
Object.keys(methods).forEach(groupName => {
  var groupMethods = methods[groupName]
%>
<h2><%= groupName %></h2>
<table>
  <tbody>
  <tr>
    <th>Method</th>
    <th>Description</th>
  </tr>
  <%
  Object.keys(groupMethods).forEach(groupMethodName => {
    var groupMethod = groupMethods[groupMethodName]
  %>
  <tr>
    <td width="30%"><a href="docs/<%=groupMethodName%>"><%= groupMethodName %></a></td>
    <td width="70%"><%= groupMethod.description %></td>
  </tr>
  <% }) %>
  </tbody>
</table>
<% }) %>
</body>
</html>
